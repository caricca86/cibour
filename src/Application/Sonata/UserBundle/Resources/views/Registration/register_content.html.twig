{% form_theme form 'CTICibourBundle:Form:register_form.html.twig' %}
<div class="panel">
    <div class="panel-heading">
        <h2 class="panel-title">{{ 'title_user_registration'|trans({}, 'SonataUserBundle') }}</h2>
    </div>
    <div class="panel-body">
        <form action="{{ path('fos_user_registration_register') }}" {{ form_enctype(form) }} method="POST" class="fos_user_registration_register form-horizontal">
            {{ form_widget(form) }}
            <div class="form-actions">
                <div class="row">
                    <div class="col-lg-8 col-sm-8 col-lg-offset-2 col-sm-offset-2">
                        <button type="submit" class="btn pull-right" style="padding: 0">
                            <img src="{{ asset('bundles/cticibour/images/icon_submit.png') }}">
                        </button>
                    </div>
                </div>
            </div>
        </form>
    </div>
</div>